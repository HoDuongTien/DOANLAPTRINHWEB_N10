@model WebQLNhanSu.Models.TrinhDo

@{
    ViewData["Title"] = "Xóa trình độ";
}

<div class="crud-page">
    <div class="card card-modern delete-card p-4 p-md-5">
        <div class="mb-4">
            <h2 class="section-title mb-1 text-danger">Xóa trình độ</h2>
            <p class="section-subtitle mb-0">Xác nhận trước khi xóa dữ liệu</p>
        </div>

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="warning-box mb-4">
            Bạn có chắc muốn xóa trình độ này không?
        </div>

        <div class="row g-4">
            <div class="col-md-6">
                <label class="form-label">Mã trình độ</label>
                <div class="detail-box">@Model.MaTrinhDo</div>
            </div>

            <div class="col-md-6">
                <label class="form-label">Tên trình độ</label>
                <div class="detail-box">@Model.TenTrinhDo</div>
            </div>
        </div>

        <form asp-action="Delete" method="post" class="page-actions">
            <input type="hidden" asp-for="MaTrinhDo" />
            <button type="submit" class="btn btn-danger">Xóa</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Quay lại</a>
        </form>
    </div>
</div>